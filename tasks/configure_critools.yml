---
# Создаем конфигурационный файл для crictl
- name: CRITOOLS | Create crictl config file
  template:
    src: crictl.yaml.j2
    dest: /etc/crictl.yaml
    owner: root
    group: root
    mode: "0644"
